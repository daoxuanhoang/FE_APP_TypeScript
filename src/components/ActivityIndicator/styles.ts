/** @format */

export default () => {
  return {
    default: {
      width: 1,
      height: 1,
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      color: "#fff",
    },
  };
};
